import logo from "./logo.svg";
import "./App.css";
// import { Box } from "@mui/material";
import functionName from "./JS/Functions";
import { Box } from "@mui/material";

function App() {
  return (
    <Box
      sx={{ alignitems: "center", display: "flex", justifyContent: "center" }}
      // sx is a prop used for applying custom styles directly to a component. 
    >
      "Welcome to Ak's Website"
    </Box>
  );
}

export default App;
